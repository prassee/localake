- set 'gc.enabled' = 'false' in load_upi_events.py and run compaction and delete orphan files 

- ensure to load data for each date as a seperate run so that it cancreate as many snapshots and versions as needed. 

